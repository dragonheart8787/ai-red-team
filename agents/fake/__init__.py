"""MVP-Kernel scripted agents: no real LLM anywhere (§10 MVP-Kernel).

The adversarial reviewer deliberately lies about risk and sensitivity so the
kernel can be proven to hold under a fully dishonest AI."""
